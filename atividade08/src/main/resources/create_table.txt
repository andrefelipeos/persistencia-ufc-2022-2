CREATE TABLE alunos (identificador SERIAL PRIMARY KEY, nome TEXT NOT NULL, matricula INTEGER UNIQUE NOT NULL, cpf TEXT UNIQUE NOT NULL CHECK (LENGTH(cpf) = 11), email TEXT UNIQUE NOT NULL, telefone TEXT UNIQUE NOT NULL);
